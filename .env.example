# Copy to `.env.local` and fill values (Next.js reads `.env.local` automatically).

# --- Site URL (important for production) ---
# AdSense, canonical URLs, sitemap, and structured data use this. Set on your host (e.g. Vercel):
# NEXT_PUBLIC_SITE_URL=https://www.startupsupdate.com
# Without it on Vercel, the app falls back to VERCEL_URL (*.vercel.app). Local dev defaults to http://localhost:3000.

# --- Google AdSense (optional) ---
# `public/ads.txt` is shipped in the repo with your site’s Google line (no env needed for that file).
# The AdSense script/meta use a built-in publisher fallback; set this to override (e.g. staging / another account):
# NEXT_PUBLIC_ADSENSE_PUBLISHER_ID=ca-pub-xxxxxxxxxxxxxxxx
#
# Easiest: one responsive display unit → use default slot for every placement:
# NEXT_PUBLIC_ADSENSE_SLOT_DEFAULT=1234567890
#
# Better for policy & reporting: unique Display → Responsive units per placement (override default):
# NEXT_PUBLIC_ADSENSE_SLOT_IN_FEED=
# NEXT_PUBLIC_ADSENSE_SLOT_SIDEBAR=
# NEXT_PUBLIC_ADSENSE_SLOT_ANCHOR=
# NEXT_PUBLIC_ADSENSE_SLOT_ARTICLE_MID=
# NEXT_PUBLIC_ADSENSE_SLOT_ARTICLE_END=
#
# Site verification troubleshooting (AdSense "Couldn't verify your site"):
# - After deploy, `/ads.txt` should show one `google.com, pub-…, DIRECT, …` line (not a # comment).
# - If you use www in AdSense, the site URL you add must match (https + host).
# - Optional: set NEXT_PUBLIC_ADSENSE_PUBLISHER_ID on the host if you need a different publisher than the code default.

ADMIN_SESSION_SECRET=long-random-string-for-signing-admin-session-cookies
ADMIN_PASSWORD=your-admin-console-password

# MongoDB Connection String — paste from MongoDB Compass (Connect → URI).
# Local (no auth) — add /database-name at the end:
# MONGODB_URI=mongodb://localhost:27017/startupupdate
# Atlas or auth:
# MONGODB_URI=mongodb+srv://USER:PASSWORD@cluster.example.mongodb.net/startupupdate?retryWrites=true&w=majority

MONGODB_URI=mongodb://localhost:27017/startupupdate
